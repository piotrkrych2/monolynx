---
name: review-history
description: Historia ocen ticketow MON-* przez Krytyka (score, iteracje, kluczowe znaleziska per ticket)
metadata:
  type: project
---

- Heartbeat MON-19: iter1=62/100 (IDOR, clock skew, dead code), iter2=78/100 (all blockers fixed, needs constants+tests+error handling)
- Heartbeat MON-20: 76/100 REQUEST CHANGES (IntegrityError blocker, validation overwrites, no resource limit, no name length check)
- Heartbeat MON-23: 88/100 APPROVED (MCP tools — missing IntegrityError handling on create, minor double-query in delete)
- Heartbeat MON-24: 88/100 APPROVED (integration tests — 18 tests, all 6 required covered, minor: no token assertion in create test)
- MCP create_project MON-25: iter1=72/100 REQUEST CHANGES (description not saved, _code_from_slug 1-char bug, no empty name validation), iter2=90/100 APPROVED (all blockers fixed, minor: magic string role, no description strip)
- MCP update_project MON-26: iter1=78/100 REQUEST CHANGES (IntegrityError race on slug, updated_at uses created_at, no length validation), iter2=90/100 APPROVED (all 3 blockers fixed)
- MCP delete_project MON-27: 90/100 APPROVED (soft-delete, owner-only, confirm guard; minor: no logger, no updated_at on Project model)
- MCP get_project MON-28: 88/100 APPROVED (full project details; minor: scalar_one race, double ProjectMember query, active_sprint duplication with get_project_summary)
- MCP create_monitor MON-29: 82/100 REQUEST CHANGES (missing name/URL length validation vs DB String(255)/String(2048), missing url.strip(), magic number 20 instead of constant)
- MCP update_monitor MON-30: 86/100 APPROVED (PATCH semantics correct, SSRF protection on URL change; minor: no name/url length validation, no url.strip() — same as create_monitor)
- MCP delete_monitor MON-31: 90/100 APPROVED (owner/admin role check, cascade delete via model, 3 tests; minor: no UUID format validation, no DB assertion in test)
- due_date MON-32: 85/100 APPROVED (clean migration, ISO date validation, 3 filters, 12 tests; medium: overdue badge on done tickets in board.html/ticket_detail.html; low: silent fallback on invalid date in dashboard forms)
- search_tickets MON-33: 85/100 APPROVED (ILIKE query+description, 6 filters, 11 tests; medium: inconsistent response format vs list_tickets, no UUID validation on sprint_id; low: incomplete docstring, missing tests for due/sprint filters)
- update_sprint MON-35: 86/100 APPROVED (PATCH semantics, date validation, completed blocker; medium: no name length validation; low: no end_date clear, no tests)
- list_members MON-36: 90/100 APPROVED (clean JOIN+case ORDER BY, email fallback; low: no User.is_active filter, no alphabetical sort test within role)
- invite_member MON-37: iter1=72/100 REQUEST CHANGES (no tests blocker, no email format validation, magic number INVITATION_DAYS, inactive user edge case)
- remove_member MON-38: 88/100 APPROVED (correct authz, owner protection, 7 tests; medium: no email format validation inconsistent with invite_member; low: no self-removal guard, no DB assertion in test)
- create_issue MON-42: iter1=62/100 REQUEST CHANGES (data discarded — description/environment/traceback built into dict but never persisted, no tests, no title length validation), iter2=88/100 APPROVED (Event created with JSONB, 17 tests, title validation; minor: source missing from list/get_issue response)
- labels MON-39: iter1=82/100 REQUEST CHANGES (IntegrityError race on create_label, search_tickets missing labels support, create/update_ticket responses lack labels), iter2=82/100 REQUEST CHANGES (frontend badges — cross-project label injection via unvalidated label_ids, no UUID dedup)
- add_attachment MON-40: 80/100 APPROVED (model+migration clean, base64/size validation, MinIO reuse; high: filename not sanitized for path traversal/header injection; medium: Content-Disposition injection; low: no attachment count limit)
- get_activity_log MON-41: 82/100 APPROVED (model+migration+service+MCP tool; medium: two DB sessions instead of one, ACTIVITY_ENTITY_TYPES in mcp_server.py; low: redundant project_id index, no dedicated tests, log_activity not called anywhere yet)
- get_burndown MON-43: 72/100 REQUEST CHANGES (no tests blocker, updated_at unreliable for actual line, forecast_completion edge case, negative days_elapsed for future sprints)
- UI attachments MON-44: 80/100 APPROVED (FilePond upload+HTMX delete, membership check on write, filename sanitized; medium: no server-side MIME validation, no attachment count limit)
- get_graph_node MON-45: 86/100 APPROVED (Cypher filters + depth_map + grouped DSL output; medium: start node depth never 0; low: no tests for new filters)
- get_graph_node testy MON-45: 88/100 APPROVED (34 testy, 5 klas; medium: wrong @pytest.mark.integration marker; low: redundancja z test_mcp_server.py TestFormatGraphDsl)
- dark/light mode MON-47: iter1=72/100 REQUEST CHANGES (4 standalone templates missing class="dark"/anti-FOUC/darkMode, landing bg-gray-950 hardcoded, logo always white on auth, status badges dark-only, toasts text-gray-900 on colored bg), iter2=88/100 APPROVED (all blockers fixed; low: status badges dark-only, landing no toggle, minor indent)
- wiki attachments MON-49: 58/100 NEEDS WORK (3 critical: page_detail missing attachments/can_edit context, files.html uses wrong variable name + wrong model attrs, _get_wiki_page missing selectinload; medium: no MIME validation, get_wiki_attachment filename ambiguity, templates.TemplateResponse instead of render_project_page)
- dashboard statusy MON-50: 88/100 APPROVED (bulk project_stats.py + projects.py paginacja/search/sort + projects.html ikonki; medium: issues_pulse logika inna niż sidebar; minor: unused field import, COALESCE inconsistency, no aria-labels on SVG)
- monitoring notifications MON-52: iter1=78/100 REQUEST CHANGES (XSS in email HTML, Slack sync blocking async, missing SSRF on Slack URL save), iter2=90/100 APPROVED (all 4 blockers fixed; db-specialist 92, backend-dev 72→88, frontend-dev 88, qa-tester 85)
- acceptance criteria MON-53: iter1=74/100 REQUEST CHANGES (lint fail B904, position bug `0 or -1`, light mode text-gray-200, dark-only form styling, dead HTMX attrs, no tests), iter2=88/100 APPROVED (all blockers fixed; low: 4x dark-only styling in template, no description length limit)
- assignee "me" MON-79: 92/100 APPROVED (3 pliki: mcp_server.update_ticket dodaje `if assignee_email=="me": assignee_email=_user.email` PRZED logika ""/lookup — placement OK; _user z _get_user_and_project nigdy None; SKILL KROK 4 ~linia 183 update_ticket dostaje assignee_email="me", KROK in_review nietkniety; test_update_ticket_assignee_me asercja `ticket.assignee_id==mcp_user.id` po refresh — niebanalna, mock_verify zwraca mcp_user; low: create_ticket/search_tickets bez "me" — poza scope. LEKCJA: git status snapshot w prompcie pokazal tylko test+memory jako modified, ale mcp_server.py+SKILL.md mialy UNSTAGED zmiany niewidoczne w `git diff --stat` snapshot z promptu — ZAWSZE rob swiezy `git diff <plik>` per docelowy plik, nie ufaj snapshotowi statusu z promptu)
- pokrycie dashboard settlements MON-81: 91/100 APPROVED (QA nowy plik test_settlements_dashboard.py 75 testow/12 klas pokrywa wszystkie 15 endpointow dashboard/settlements.py. RBAC delete-vs-write trafione: admin w DEFAULT_ROLE_PERMISSIONS ma rozliczenia=[read,write] BEZ delete -> delete/delete-attachment endpointy daja 403 dla admina; member ma rozliczenia=[] -> 403 nawet na read. 404 cross-project przez M2M (_get_settlement join SettlementProject). search 403 jako HTMLResponse (check_permission, nie require). Mock minio patch monolynx.services.settlements.minio_client.* — poprawny. Izolacja czysta: zaden email >2x. Drobne: mylacy docstring upload_signed (testuje .exe nie state), zbedny patch upload_object w create-happy, brak paginacji >20. PULAPKA: Pyright zglosil `from datetime import UTC` jako unknown — FALSZYWY ALARM, projekt Python 3.12 (datetime.UTC od 3.11), identyczny import w test_settlements_workflow.py dziala, fix niepotrzebny. RBAC rozliczenia: owner=read+write+delete, admin=read+write, member=[].)
- pokrycie MCP server MON-80: iter1=76/100 NEEDS WORK (QA 72 testy/17 klas, 344 passed, wysoka jakosc — assertions konkretne, mock_factory/mock_verify/_make_ctx OK, sciezki bledow pelne; BLOCKER: pominiety caly docelowy blok 4315-4674 = 8 tooli MCP z 0 testow (get_burndown, get_attachment, get_wiki_attachment, add_wiki_page_attachment, add_wiki_file, get_wiki_file, update_wiki_file, list_wiki_files) → cel 95% nieosiagniety; QA pokryl zamiast tego role tools 4884-5191. Lekcja: ZAWSZE mapuj docelowe zakresy linii na faktyczne def-y (awk/grep) i grep czy toole maja >0 referencji w tescie — QA moze pokryc inny niepokryty region niz docelowy). iter2=90/100 APPROVED (QA dodal 26 testow/8 klas dla bloku 4315-4674: get_burndown/get_attachment/get_wiki_attachment/add_wiki_page_attachment/add_wiki_file/get_wiki_file/update_wiki_file/list_wiki_files; weryfikacja: patch-targety svc_get_burndown_data/minio_get_attachment/minio_upload_object zaimportowane w namespace mcp_server -> patche dzialaja; trafione not-found+data-URI+walidacja; 98 testow lacznie. Lekcja re-review: grep patch-target w `import` zrodla, zeby potwierdzic ze patch faktycznie podmienia uzywana funkcje, nie tylko ze test istnieje)

- pokrycie features.py + main.py lifespan MON-86: 90/100 APPROVED (QA 2 nowe pliki: test_features.py 202l, test_main_lifecycle.py 264l, brak zmian prod. Kluczowe TRAFIONE: lifespan() patchuje ensure_bucket/graph.init_driver/init_schema/close_driver/monitor_checker_loop/session_manager.run — cele trafiaja w LAZY importy WEWNATRZ lifespan (from monolynx.services.X import Y) patchowane U ZRODLA modulu -> poprawnie, zaden test nie dotyka realnego MinIO/Neo4j. session_manager.run side_effect=lambda: fresh async CM per call. SKIP_LANDING_PAGE via monkeypatch.setattr("monolynx.main.settings.SKIP_LANDING_PAGE",...) auto-restore izolacja; endpointy .md NIE gated wiec slusznie bez monkeypatch. Parytet 10 modulow: parametryzacja po ALL_SLUGS=list(_FEATURES.keys()) — dynamiczna, lapie pipelines (brak w opisie ticketu); guardy matching-keys + rowna liczba mcp_tools/features PL vs EN. lifespan 5 wariantow: happy(ENABLE_MONITOR_LOOP=True)+3 exception-swallowed(minio/neo4j-init/neo4j-close)+loop-disabled(False). _register_routers: assert na main_app.routes bez ponownego wywolania (unika duplikatow). Nity: mcp root-mount "" tylko w komentarzu (assert tylko "/mcp"); brak assercji na brak duplikatow route; ai_intro licznik nie asertowany numerycznie vs len(mcp_tools). Review STATYCZNY — nie odpalono pytest --cov (numpy crash + zasada: testy odpala user).)

- pokrycie serwisow dolnych MON-87 (2 rownolegle qa-testerzy): A=94/100, B=93/100, oba APPROVED. Strumien A (realna baza, db_session/test_project/engine): test_burndown_service.py (nowy 253l) — liczy wzgledem realnego date.today() z offsetami (start=today-5) zamiast patchowac (zgodnie z researcherem); pokryte no-tickets/mixed/no-done/fallback-end_date+14/completed(velocity round(5/7,1)=0.7)/lookup active-None-nonexistent/days_total==0. test_activity_service.py (nowy 198l) — pola+filtry get_activity_log + KLUCZOWY test flush-not-commit: otwiera niezalezne engine.connect() i sprawdza count==0; poniewaz db_session bound bezposrednio do connection z jawnym begin() (nie savepoint), dodanie commit() do serwisu ZEPSULOBY test -> twardy guard na db-commit.md, nie tylko dokumentacja. test_oauth.py (+123l) — 4 galezie ProgrammingError->503 zgodne z api/oauth.py:83/122/172/281; helper _raise_programming_error_once rzuca RAZ i deleguje (patch tylko jedno execute). Strumien B (mocki, bez bazy): test_notifications.py (+150l) pelna matryca SSRF z patchem socket.getaddrinfo (zero realnego DNS), debounce aware+naive; test_cli.py (+140l) createsuperuser wszystkie galezie + KLASA REGRESYJNA egzekwuje bezargumentowe async def coroutines w COMMANDS (inspect.signature len==0 + iscoroutinefunction) na makefile-cli.md; test_monitor_loop.py (+161l) _check_single_monitor/run_monitor_checks/petla (lock acquired+not-acquired assert close/dispose x2); test_minio_client.py (+130l) singleton reset _client=None w setup/teardown -> brak przeciekania. Wszystkie 7 py_compile OK, 0 em-dashy. Drobne nieblokujace: A helper zaklada ze pierwszy execute=lookup OAuthClient (kruche); B test_loop_handles_exception nie weryfikuje 2.iteracji (realny sleep(10), await_count>=1 uczciwie skomentowane). Zastrzezenie: coverage --cov crashuje (numpy) -> review statyczny/jakosciowy.

- Connections edge provenance MON-117: 90/100 APPROVED (confidence/source_relation krawedzi w metadata Neo4j, zero migracji). constants GRAPH_EDGE_CONFIDENCE=(EXTRACTED/INFERRED/AMBIGUOUS). schemas/graph.py: GraphEdgeCreate+confidence/source_relation+to_metadata()(pola jawne wygrywaja)+validate_confidence()(bool jak validate_type); GraphEdgeResponse+model_post_init(v2 hook OK) wyciaga z metadata. Schematy CELOWO dokumentacyjne — serwis/MCP operuja na dictach, confidence plynie przez arg metadata (passthrough MON-116) -> validate_confidence() NIE egzekwowany w sciezce zapisu (bledny confidence trafi do Neo4j bez walidacji; low sev, graphify zaufany, UI escapuje). Template connections/index.html: edge tap (removeClass+selectedNode=null), showEdgeDetail wszystko przez escapeHtml (fallback CONFIDENCE_LABELS[x]||x WEWNATRZ escapeHtml; edgeColor=EDGE_COLORS[type]||stala, brak surowej wartosci w style), klasa 'inferred' dashed nie koliduje z dimmed/highlighted (rozne wlasciwosci), detailPanelTitle przelaczany. Backwards-compat: edge bez metadata->meta={}->brak throw/klasy. Testy: 6 TestEdgeProvenance + test_create_edge_with_provenance_metadata (asercja json.dumps w Cypher) + integration passthrough. PULAPKA STACKINGU: branche feature-116/117 na tym samym commicie, CALA praca 116+117 NIEZACOMMITOWANA w working tree -> `git diff feature-116-replace-graph` NIE byl czysty MON-117 (zawieral MON-116: replace_graph service+tool+features.py 9->10). Weryfikacja: `git show base:plik | grep -c symbol`=0 ORAZ `git log base..HEAD` pusty -> baza nie ma pracy spod. features.py zmiana nalezy do MON-116 i zgodna z rule landing-page-features.md; MON-117 sam nie dodaje toola wiec features.py niepotrzebne. Zawsze grep symbolu w base zamiast ufac nazwie brancha.
